config snmpd-librenms general
	option enabled '1'
	# seconds to coalesce bss.add/bss.remove bursts before reconciling
	option debounce '5'
	# minimum seconds between two flash writes (trailing re-run if deferred)
	option cooldown '30'
